## 1. Update HistoryView.vue Template

- [x] 1.1 Replace Bootstrap grid classes (`row`, `col-md-*`) with custom semantic classes
- [x] 1.2 Replace Bootstrap card classes (`card`, `card-body`, `card-title`, `card-text`, `card-img-top`) with semantic classes
- [x] 1.3 Replace Bootstrap form classes (`form-control`, `form-select`) with semantic classes
- [x] 1.4 Replace Bootstrap button classes (`btn`, `btn-outline-secondary`) with semantic classes
- [x] 1.5 Replace Bootstrap utility classes (`text-muted`, `text-center`, `mb-4`, etc.) with semantic classes
- [x] 1.6 Update progress bar structure to work with custom CSS

## 2. Add Scoped CSS Styles

- [x] 2.1 Add root container styles using `var(--bg-primary)`
- [x] 2.2 Add filter section card styles using CSS variables
- [x] 2.3 Add form input and select styles using CSS variables
- [x] 2.4 Add button styles using CSS variables
- [x] 2.5 Add history card styles using CSS variables for background, border, shadow
- [x] 2.6 Add progress bar styles using CSS variables
- [x] 2.7 Add badge styles using CSS variables
- [x] 2.8 Add text color styles for primary, secondary, and muted text
- [x] 2.9 Add hover effects using CSS variables

## 3. Verification

- [x] 3.1 Test dark mode toggle on history page
- [x] 3.2 Verify all colors switch correctly between light and dark modes
- [x] 3.3 Verify existing functionality (search, filter, sort) still works
- [x] 3.4 Check responsive layout on mobile and desktop
